export function ReportRenderer({ markdown }: { markdown: string }) {
    return (
        <div className="prose prose-invert max-w-none">
            {/* TODO: Implement react-markdown */}
        </div>
    );
}
